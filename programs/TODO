Add second pass after tokenizing to insert STARTEX and ENDEX tokens appropriately
- It shouldn't add tokens to single immediates
- It should add those tokens to expressions within AND without parenthesis
